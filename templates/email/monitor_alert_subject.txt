{% with c=changes.0 %}{% if c.change_type == "status_down" %}⚠ {{ target.name }} está OFFLINE — AttackFlow{% elif c.change_type == "ssl_expiry_warn" %}⏳ SSL expirando en {{ target.name }} — AttackFlow{% elif c.change_type == "header_removed" %}🔓 Header de seguridad eliminado en {{ target.name }} — AttackFlow{% else %}🔔 Cambio detectado en {{ target.name }} — AttackFlow{% endif %}{% endwith %}